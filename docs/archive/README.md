# Archive

Docs in this folder are reference only.

They are not active Codex instructions, runbooks, build order, or current repo state. If archived docs conflict with `AGENTS.md`, `PROJECT_STATE.md`, or `MILESTONES.md`, the active files win.

Do not run commands from archived docs unless a current milestone and `PROJECT_STATE.md` explicitly bless them.

Archiving is documentation status only. Content is retained for provenance.
